<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <Button
        android:id="@+id/selectPotButton"
        android:layout_width="370dp"
        android:layout_height="70dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:text="Selected Pot" />

    <TextView
        android:id="@+id/outputTemp"
        android:layout_width="180dp"
        android:layout_height="100dp"
        android:text="온도"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/selectPotButton"
        android:layout_alignStart="@+id/selectPotButton"/>

    <TextView
        android:id="@+id/outputHum"
        android:layout_width="180dp"
        android:layout_height="100dp"
        android:text="습도"
        android:layout_alignTop="@+id/outputTemp"
        android:layout_alignEnd="@+id/selectPotButton"/>

    <Button
        android:id="@+id/controlSensor"
        android:layout_width="370dp"
        android:layout_height="70dp"
        android:text="Control Sensor"
        android:layout_below="@+id/outputHum"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp" />
</RelativeLayout>